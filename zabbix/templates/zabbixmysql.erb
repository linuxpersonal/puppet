#!/bin/bash

mysql -e create database <%= @zabbixdb %> character set utf8 collate utf8_bin;
mysql -e grant all privileges on zabbixdb.* to <%= @zabbixuser %>@localhost identified by '<%= @mysqlpass %>';
mysql -e quit;
